#define WL_DEF_BUFFER_TARGET(target) constexpr static GLenum s_target = target
#define AllocatorImpl(name, id)          \
    enum class id : GLuint { Null = 0 }; \
    struct name : Allocator<name> {      \
        using Id = id;                   \
    };
